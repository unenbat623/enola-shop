name = "enola-shop"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]

# Cloudflare Pages configuration
pages_build_output_dir = "dist"

# Cloudflare Workers configuration (SPA fallback)
[site]
bucket = "./dist"
